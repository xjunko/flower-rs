[package]
name = "apps-template"
version.workspace = true
edition.workspace = true

[[bin]]
bench = false
name = "apps-template"
test = false

[dependencies]
flower-libc = { path = "../../flower-libc" }
