[package]
name = "flower-libc"
version = "0.0.1"
edition = "2024"

[lib]
bench = false
test = false
crate-type = ["staticlib", "rlib"]

[dependencies]
flower-uapi = { path = "../flower-uapi" }
linked_list_allocator = "0.10.5"
spin = "0.10.0"

[features]
debug = []
